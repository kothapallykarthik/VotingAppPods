apiVersion: v1

kind: Pod

metadata:
  name: votingapp-pod
  labels:
    name: myvoting-app
    for: votingapp-viapods

spec:
  containers:
    - name: votingapp-image
      image: dockersamples/examplevotingapp_vote
      ports:
        - containerPort: 80

