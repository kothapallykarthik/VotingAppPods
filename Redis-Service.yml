apiVersion: v1

kind: Service

metadata:
  name: redis-service
  labels:
    name: myvoting-app
    for: votingapp-viapods

spec:
  ports:
    - targetPort: 6379
      port: 6379

  selector:
    name: myredis-app
    for: votingapp-viapods

